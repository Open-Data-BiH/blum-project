---
import { getLineDetailPath, type Locale } from '../lib/site-config';
import '../styles/pages/_updates.scss';
import updatesData from '../../public/data/transport/updates.json';
import ownershipData from '../../public/data/transport/routes/urban_company_ownership.json';
import type { Category, Update } from '../types/updates';
import type { CompanyOwnership } from '../types/lines';

interface Props {
  locale?: Locale;
}

const { locale = 'bhs' } = Astro.props;
const { categories, updates } = updatesData as { categories: Category[]; updates: Update[] };
const existingLineIds = new Set(
  ((ownershipData as CompanyOwnership).urban ?? []).flatMap((company) => company.lines.map((line) => line.lineId)),
);
const isEnglish = locale === 'en';
const severityLabel = (severity: Update['severity']): string => {
  if (severity === 'warning') {
    return isEnglish ? 'Warning' : 'Upozorenje';
  }
  if (severity === 'urgent') {
    return isEnglish ? 'Urgent' : 'Hitno';
  }
  return 'Info';
};
---

<div class="updates-container" id="updates-section">
  <!-- Filter buttons — rendered server-side, activated/toggled via TS -->
  <div class="updates-filters" id="updates-filters" role="group" aria-label="Filter updates">
    <button class="updates-filter-btn active" data-filter="all">
      <i class="fas fa-list" aria-hidden="true"></i>
      {isEnglish ? 'All' : 'Sve'}
    </button>
    {categories.map((cat) => (
      <button class="updates-filter-btn" data-filter={cat.id}>
        <i class={`fas ${cat.icon}`} aria-hidden="true"></i>
        {cat.label[locale]}
      </button>
    ))}
  </div>

  <!-- Update cards — rendered server-side, hidden/shown via TS filter -->
  <div id="updates-content">
    {(() => {
      const activeUpdates = updates.filter((u) => u.isActive);
      if (activeUpdates.length === 0) {
        return (
          <div class="updates-empty">
            <i class="updates-empty__icon fas fa-check-circle" aria-hidden="true"></i>
            <h3 class="updates-empty__title">{isEnglish ? 'No Active Updates' : 'Nema aktivnih obavještenja'}</h3>
            <p class="updates-empty__text">
              {isEnglish
                ? 'There are currently no active updates or announcements.'
                : 'Trenutno nema aktivnih obavještenja.'}
            </p>
          </div>
        );
      }

      return (
        <div class="updates-list">
          {activeUpdates.map((update) => {
            const cat = categories.find((c) => c.id === update.type);
            const catIcon = cat ? cat.icon : 'fa-info-circle';

            return (
              <article
                class={`update-card update-card--${update.severity}`}
                id={`update-${update.id}`}
                data-update-id={update.id}
                data-type={update.type}
                data-published={update.datePublished}
                data-expiry={update.dateExpiry ?? ''}
                data-severity={update.severity}
              >
                <div class="update-card__header">
                  <div class="update-card__badges">
                    <span class={`update-badge update-badge--${update.type}`}>
                      <i class={`fas ${catIcon}`} aria-hidden="true"></i>
                      {cat && cat.label[locale]}
                    </span>
                    <span class={`update-badge update-badge--severity-${update.severity}`}>
                      {severityLabel(update.severity)}
                    </span>
                  </div>
                  <div class="update-card__date">
                    <i class="fas fa-calendar" aria-hidden="true"></i>
                    <span class="update-card__date-text" data-date={update.datePublished}>
                      {new Date(update.datePublished).toLocaleDateString(locale === 'en' ? 'en-BA' : 'bs-BA', { day: '2-digit', month: '2-digit', year: 'numeric' })}
                    </span>
                  </div>
                </div>

                <h3 class="update-card__title">{update.title[locale]}</h3>
                <p class="update-card__description">{update.description[locale]}</p>

                {update.affectedLines && update.affectedLines.length > 0 && (
                  <div class="update-card__lines">
                    <span class="update-card__lines-label">{isEnglish ? 'Affected lines:' : 'Zahvaćene linije:'}</span>
                    {update.affectedLines.map((line) => {
                      const lineId = String(line);
                      return existingLineIds.has(lineId) ? (
                        <a href={getLineDetailPath(locale, lineId)} class="line-badge">{lineId}</a>
                      ) : (
                        <span class="line-badge">{lineId}</span>
                      );
                    })}
                  </div>
                )}

                <div class="update-card__footer">
                  <div class="update-card__source">
                    {isEnglish ? 'Source:' : 'Izvor:'}
                    {update.sourceUrl
                      ? <a href={update.sourceUrl} target="_blank" rel="noopener noreferrer">
                          {update.source}
                          <i class="fas fa-external-link-alt" aria-hidden="true"></i>
                        </a>
                      : update.source
                    }
                  </div>
                  {update.dateExpiry && (
                    <div class="update-card__expiry">
                      <i class="fas fa-clock" aria-hidden="true"></i>
                      <span>
                        {isEnglish ? 'Valid until:' : 'Važi do:'}
                        {' '}{new Date(update.dateExpiry).toLocaleDateString(locale === 'en' ? 'en-BA' : 'bs-BA', { day: '2-digit', month: '2-digit', year: 'numeric' })}
                      </span>
                    </div>
                  )}
                </div>
              </article>
            );
          })}
        </div>
      );
    })()}
  </div>
</div>

<script>
  const updatesSectionEl = document.getElementById('updates-section');
  if (updatesSectionEl) {
    const observer = new IntersectionObserver(
      (entries) => {
        if (entries[0].isIntersecting) {
          observer.disconnect();
          import('../scripts/features/updates/updates-filter').then(({ initUpdatesFilter }) => {
            initUpdatesFilter();
          });
        }
      },
      { rootMargin: '300px' },
    );
    observer.observe(updatesSectionEl);
  }
</script>
